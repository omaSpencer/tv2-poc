/** The fixture lives with the application so the demo runner and the tests
 *  cannot drift apart. */
export {
  DEMO_CONTENT, DEMO_EDIT, DEMO_SLUG, DEMO_WITHDRAWN_EDIT, NEGATIVE_CASES,
} from '../../src/content/demo-fixture.js';
