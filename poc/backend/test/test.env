# Intentionally contains no runtime configuration.
# Vitest injects the feature-off baseline and each integration harness enables
# only the adapter it owns. This prevents a developer's .env from leaking into
# isolated tests.
