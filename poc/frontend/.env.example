# Browser-visible. Empty API base = same-origin `/api` (Vite proxy in `npm run dev`).
VITE_API_BASE=/api

# Used only by the Vite dev proxy (not exposed to the browser bundle).
VITE_BACKEND_ORIGIN=http://127.0.0.1:3000
